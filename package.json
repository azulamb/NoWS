{
  "name": "nows",
  "version": "0.0.1",
  "description": "Node.js Web Server",
  "main": "index.js",
  "scripts": {
    "daemon": "",
    "start": "node ./server/index.js",
    "build": "tsc -p src/",
    "build:js": "tsc -p docs/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hiroki",
  "license": "MIT",
  "devDependencies": {
    "@types/json5": "0.0.30",
    "@types/node": "^10.12.0"
  },
  "dependencies": {
    "json5": "^2.1.0"
  }
}
